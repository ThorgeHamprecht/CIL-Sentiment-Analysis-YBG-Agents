# Declaration of AI:

## Report
For the written report we used AI to help with writing. During the writing process it was used to help the authors find the right wording, however all initial texts were self written by the authors. Later during the peer review process, where we helped each other rephrase sentences and shorten paragraphs to keep within the 4 page limit, AI was used to help with rephrasing and shortening. All the arguments however were written by the authors and the AI was only used to help with the wording. The final report was proofread by all authors and all changes were accepted by all authors.

## Code
For the code we involved Generative AI to help with writing code and iterrating on experiments. Each person used AI differently, in general it was used to help with "busy work". That included writing jobs submission scripts and writing code to generate plots. AI was also used in the development process to help with writign code for experiments, this was mostly to fill in functions after the author wrote function signatures and base implementation. AI was used for faster itteration on experiments, by for example, based on existing code, adding parameter sweep functionality to a script via CLI args oder similar. *However all ideas we tested in this project were came up with, by the authors and AI was only used to help with the implementation of those ideas.* Also in the end we let AI clean up our whole research repo into one clean structure, which is the one we submit. This was done so our experiments follow the same structure and are easier to find and replicate, thus in the end, all code is somewhat AI affected in different proportions
Tool used:
- Codex with GPT 5.5 and 5.4 in Medium and "Extra-High" tier and Plan-Mode Feature
- GitHub copilot student plan
Files affected: all bash submit scripts and make_sontrastive_report_plot.py are fully AI generated.
Purpose: [e.g., Used to translate Jupyter notebook cells into Python scripts and to generate the regex for data cleaning.]